# Copia in .env e personalizza. NON committare il .env reale.
# Token bearer condiviso con Lovable (usa un valore lungo e casuale in produzione).
MCP_TOKEN=dev-change-me-please

# Rete del server HTTP
HOST=0.0.0.0
PORT=8000
